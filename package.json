{
  "name": "keyword-status",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "collect": "node src/collect-daily.mjs",
    "snapshot": "node src/collect-daily.mjs --snapshot-only",
    "report": "node src/generate-report.mjs",
    "check": "node src/generate-report.mjs --check",
    "train": "node src/train-model.mjs",
    "test": "node --test"
  }
}
